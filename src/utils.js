

export function getTicketById(columns, ticketId) {
  // let ticket;
  // Object.entries(columns).forEach(([id, column]) => {
  //   column.tasks.forEach(task => {
  //     console.log(task.id);
  //     console.log(id);
  //     if (task.id === ticketId) {
  //       ticket = task;
  //     }
  //   })
  // })

  return ticket
}